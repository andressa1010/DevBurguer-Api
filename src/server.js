import app from "./app.js"
// Importa a aplicação configurada no arquivo app.js


// Inicializa o servidor e define a porta para escutar requisições
app.listen(3001, ()=>{
    console.log("Servidor em execução na porta http://localhost:3001")
// Exibe uma mensagem no console indicando que o servidor está em execução
})



